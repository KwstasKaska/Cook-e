query Chef($id: Int!) {
  chef(id: $id) {
    id
    bio_el
    bio_en
    user {
      id
      username
      image
    }
    recipes {
      id
      title_el
      title_en
      recipeImage
      difficulty
    }
  }
}
