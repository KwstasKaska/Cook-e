query Nutritionists($limit: Int = 10, $offset: Int = 0) {
  nutritionists(limit: $limit, offset: $offset) {
    id
    bio
    phone
    city
    user {
      id
      username
      email
      image
    }
  }
}
